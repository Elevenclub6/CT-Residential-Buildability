const extraCss=document.createElement('link');extraCss.rel='stylesheet';extraCss.href='enhancements.css';document.head.appendChild(extraCss);
const seeds={"East Lyme":82,Killingly:78,Lebanon:76,Plainfield:74,Voluntown:72,Sterling:70,Canterbury:69,Scotland:68,Hampton:66,Brooklyn:64,Groton:61,Middletown:58};let features=[],selected=null,geoLayer,labelLayer,scoreShift=0;const edits=JSON.parse(localStorage.getItem('ctBuildabilityEdits')||'{}');
const ctBounds=L.latLngBounds([40.93,-73.74],[42.08,-71.72]);const map=L.map('map',{zoomControl:true,attributionControl:true,maxBounds:ctBounds,maxBoundsViscosity:1,minZoom:8,zoomSnap:.25}).fitBounds(ctBounds);map.attributionControl.setPrefix('Municipal geometry: U.S. Census Bureau');labelLayer=L.layerGroup().addTo(map);
const hash=s=>[...s].reduce((a,c)=>(a*31+c.charCodeAt(0))>>>0,7);const base=n=>edits[n]?.score??seeds[n]??(42+hash(n)%39);const score=n=>Math.max(0,Math.min(100,Number(base(n))+scoreShift));const color=s=>s>=81?'#16b978':s>=61?'#70d35d':s>=41?'#ecd84f':s>=21?'#ef8541':'#d83247';
const metrics=n=>{let h=hash(n),d={"Land availability":45+h%48,"Land cost":40+(h>>3)%51,"Environmental fit":42+(h>>6)%48,"Infrastructure":40+(h>>9)%52,"Accessibility":38+(h>>12)%55,"Tax environment":40+(h>>15)%49,"Growth outlook":38+(h>>18)%54};return {...d,...(edits[n]?.metrics||{})}};
function townName(f){return (f.properties.BASENAME||f.properties.NAME||'Municipality').replace(/ town$/i,'')}
function style(f){const s=score(townName(f));return {color:'#e8f1f5',weight:selected===f?3:1,fillColor:color(s),fillOpacity:.78}}
function draw(){if(geoLayer)geoLayer.remove();labelLayer.clearLayers();geoLayer=L.geoJSON({type:'FeatureCollection',features},{style,onEachFeature:(f,l)=>{const n=townName(f);l.bindTooltip(`${n} · ${score(n)}/100`,{sticky:true});l.on('click',()=>select(f));if(document.getElementById('labels').checked&&map.getZoom()>=8.5){const c=l.getBounds().getCenter();L.marker(c,{interactive:false,icon:L.divIcon({className:'town-label',html:n,iconSize:[90,16],iconAnchor:[45,8]})}).addTo(labelLayer)}}}).addTo(map);const exact=geoLayer.getBounds();map.setMaxBounds(exact.pad(.02));map.fitBounds(exact,{padding:[15,15]});map.setMinZoom(map.getZoom());renderLists()}
async function loadMap(){const u='https://tigerweb.geo.census.gov/arcgis/rest/services/TIGERweb/Places_CouSub_ConCity_SubMCD/MapServer/1/query?where=STATE%3D%2709%27&outFields=BASENAME%2CNAME%2CGEOID&returnGeometry=true&outSR=4326&f=geojson';try{const r=await fetch(u);if(!r.ok)throw Error();const g=await r.json();features=g.features.sort((a,b)=>townName(a).localeCompare(townName(b)));draw();document.getElementById('loading').remove()}catch(e){document.getElementById('loading').textContent='Municipal boundary service is temporarily unavailable. Refresh to try again.'}}
function select(f){selected=f;const n=townName(f),s=score(n),m=metrics(n),e=edits[n]||{};document.getElementById('townName').textContent=n;document.getElementById('townSub').textContent='Connecticut municipality · Census GEOID '+(f.properties.GEOID||'—');document.getElementById('score').textContent=s;document.getElementById('summary').textContent=e.notes||`${n} is ranked as a preliminary screening candidate under the current preferences. The score is a prototype planning signal—not a determination that any parcel is buildable.`;document.getElementById('metrics').innerHTML=Object.entries(m).map(([k,v])=>`<div class="metric"><span>${k}</span><div class="bar"><i style="width:${v}%;background:${color(v)}"></i></div><b>${v}</b></div>`).join('');document.getElementById('facts').innerHTML=`<div><small>Current rank</small><b>#${ranked().findIndex(x=>townName(x)===n)+1}</b></div><div><small>Population</small><b>${e.population||'Not entered'}</b></div><div><small>Land value</small><b>${e.landValue||'Not entered'}</b></div><div><small>Data status</small><b>${edits[n]?'User edited':'Prototype'}</b></div>`;document.querySelectorAll('#editTown,#parcels,#profile').forEach(x=>x.disabled=false);draw()}
function ranked(filter=''){return features.filter(f=>townName(f).toLowerCase().includes(filter.toLowerCase())).sort((a,b)=>score(townName(b))-score(townName(a)))}
function rows(list){return `<div class="rank head"><span>#</span><span>Municipality</span><span>Score</span><span>Relative result</span></div>`+list.map((f,i)=>{const n=townName(f),s=score(n);return `<div class="rank" data-town="${n}"><span>${i+1}</span><b>${n}</b><span>${s}</span><span class="bar"><i style="width:${s}%;background:${color(s)}"></i></span></div>`}).join('')}
function renderLists(){document.getElementById('topRankings').innerHTML=rows(ranked().slice(0,10));document.getElementById('fullRankings').innerHTML=rows(ranked(document.getElementById('search').value));document.querySelectorAll('.rank[data-town]').forEach(r=>r.onclick=()=>{const f=features.find(x=>townName(x)===r.dataset.town);showView('map');map.fitBounds(L.geoJSON(f).getBounds(),{maxZoom:11});select(f)});renderCompare()}
function showView(v){document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));document.getElementById(v+'View').classList.add('active');document.querySelectorAll('nav button').forEach(x=>x.classList.toggle('active',x.dataset.view===v));document.querySelector('.table').style.display=v==='map'?'block':'none';if(v==='map')setTimeout(()=>map.invalidateSize(),20)}
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>showView(b.dataset.view));document.getElementById('budget').oninput=e=>document.getElementById('budgetOut').textContent='$'+Math.round(e.target.value/1000)+'K';document.getElementById('analyze').onclick=()=>{scoreShift=(document.getElementById('budget').value-150000)/25000+(document.getElementById('availability').selectedIndex-1)*3-(document.getElementById('tax').selectedIndex-1)*2;draw()};document.getElementById('reset').onclick=()=>location.reload();document.getElementById('labels').onchange=e=>e.target.checked?draw():labelLayer.clearLayers();document.getElementById('boundaries').onchange=e=>{if(geoLayer)geoLayer.setStyle(f=>({...style(f),weight:e.target.checked?1:0}))};document.getElementById('search').oninput=renderLists;document.getElementById('quickSearch').oninput=e=>{showView('rankings');document.getElementById('search').value=e.target.value;renderLists()};document.getElementById('closeDetail').onclick=()=>{selected=null;document.getElementById('townName').textContent='Select a town';document.getElementById('score').textContent='—';draw()};
const modal=document.getElementById('modal');document.getElementById('closeModal').onclick=()=>modal.close();document.getElementById('editTown').onclick=openEditor;document.getElementById('parcels').onclick=()=>openModal('parcels');document.getElementById('profile').onclick=()=>openModal('profile');function openModal(type){const n=townName(selected);document.getElementById('modalBody').innerHTML=type==='parcels'?`<h2>${n}: Candidate Parcel Research</h2><p>This view is now functional as a research workflow. Parcel ranking will activate when parcel/CAMA and constraint layers are connected.</p><ol class="steps"><li>Filter parcels by minimum lot size and assessed land value.</li><li>Remove known flood, wetland, protected-land and steep-slope conflicts.</li><li>Check zoning, frontage, utilities and local regulations.</li><li>Rank surviving parcels and export a due-diligence shortlist.</li></ol><p><b>No candidate parcels are claimed yet.</b></p>`:`<h2>${n} Municipal Profile</h2><p><b>Current score:</b> ${score(n)}/100</p><p><b>Census GEOID:</b> ${selected.properties.GEOID||'—'}</p><p><b>Your notes:</b> ${edits[n]?.notes||'None entered'}</p><p><a href="https://www.google.com/search?q=${encodeURIComponent(n+' Connecticut zoning regulations')}" target="_blank">Research ${n} zoning ↗</a></p>`;modal.showModal()}
function openEditor(){const n=townName(selected),e=edits[n]||{},m=metrics(n);document.getElementById('modalBody').innerHTML=`<h2>Edit ${n}</h2><p>Entries save in this browser and replace prototype values for this municipality.</p><form id="editForm" class="editForm"><label>Overall score<input name="score" type="number" min="0" max="100" value="${e.score??score(n)}"></label><label>Population<input name="population" value="${e.population||''}" placeholder="e.g. 18,750"></label><label>Land value<input name="landValue" value="${e.landValue||''}" placeholder="e.g. $140,000"></label>${Object.entries(m).map(([k,v])=>`<label>${k}<input name="m_${k}" type="number" min="0" max="100" value="${v}"></label>`).join('')}<label class="wide">Research notes<textarea name="notes" rows="4" placeholder="Zoning, utilities, sources, opportunities…">${e.notes||''}</textarea></label><div class="wide editActions"><button class="primary" type="submit">Save Municipality</button><button id="clearTown" class="secondary" type="button">Restore Prototype</button></div></form>`;modal.showModal();document.getElementById('editForm').onsubmit=ev=>{ev.preventDefault();const d=new FormData(ev.target),customMetrics={};for(const k of Object.keys(m))customMetrics[k]=Math.max(0,Math.min(100,Number(d.get('m_'+k))));edits[n]={score:Number(d.get('score')),population:d.get('population'),landValue:d.get('landValue'),notes:d.get('notes'),metrics:customMetrics};localStorage.setItem('ctBuildabilityEdits',JSON.stringify(edits));modal.close();select(selected)};document.getElementById('clearTown').onclick=()=>{delete edits[n];localStorage.setItem('ctBuildabilityEdits',JSON.stringify(edits));modal.close();select(selected)}}
function renderCompare(){const current=[...document.querySelectorAll('.cmp')].map(x=>x.value),options=ranked().map(f=>`<option>${townName(f)}</option>`).join('');document.getElementById('comparePickers').innerHTML=[0,1,2].map((_,i)=>`<select class="cmp"><option value="">Select town ${i+1}</option>${options}</select>`).join('');document.querySelectorAll('.cmp').forEach((x,i)=>{x.value=current[i]||'';x.onchange=updateComparison});updateComparison()}function updateComparison(){const ns=[...document.querySelectorAll('.cmp')].map(x=>x.value).filter(Boolean),keys=['Buildability score',...(ns[0]?Object.keys(metrics(ns[0])):[])];document.getElementById('comparison').innerHTML=ns.length?`<table class="compareTable"><thead><tr><th>Metric</th>${ns.map(n=>`<th>${n}</th>`).join('')}</tr></thead><tbody>${keys.map(k=>`<tr><td>${k}</td>${ns.map(n=>`<td class="${k==='Buildability score'?'compareScore':''}">${k==='Buildability score'?score(n):metrics(n)[k]}</td>`).join('')}</tr>`).join('')}</tbody></table>`:''}
loadMap();
